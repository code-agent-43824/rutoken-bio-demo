# PLAN — текущий этап

**Этап «Переосмысление дизайна» закрыт.** Владелец смотрит результат сам (§13).

Функциональность владельца устраивает; меняются подача, тексты и поведение списка ключей.

## Контент и тексты

- [x] «Рутокен» везде по-русски — в логотипе шапки латиница
- [x] Убрать заголовок «Как пользоваться» и подпись под ним — очевидны без слов
- [x] Убрать из hero обе ссылки-кнопки (на шаги и на журнал)
- [x] Добавить в hero: первый Рутокен со встроенным отпечатком пальца, запоминает до 3 пальцев
- [x] Убрать строку про криптопроцессор из карточки устройства
- [x] Шаг 3: «С использованием биометрии или без использования биометрии»
- [x] Шаг 4: убрать состояние «не определено» — таких ключей не бывает
- [x] Шаг 5: «При подписи на ключе с использованием биометрии появится попап»

## Поведение

- [x] Шаг 2: PIN показывать открытым текстом. **Решение владельца**, не «исправлять»
      обратно на `type="password"` — записать в «Settled decisions»
- [x] Список ключей обновляется принудительно после создания и после удаления ключа.
      Отменяет прежнее решение «только по кнопке»: его отменил владелец. `test.js`
      проверяет обратное — тест и код правятся одним коммитом (§10)

## Дизайн

- [x] Пять карточек в две строки (3 + 2) — сейчас пять в ряд, и они слишком узкие
- [x] Попап переделать: не пытаться рисовать форму токена, а показать отпечаток пальца,
      который сканируется и при успехе становится зелёным
- [x] Общая подача: типографика, акценты, карточки — менее шаблонно

## Как проверено

`node test.js` зелёный. Рендер в Chromium на 1440, 1100, 900, 768 и 390 px:
горизонтального скролла нет, шаги идут 3 + 2 → 2 + 2 + 1 → по одному, попап и его
зелёное состояние помещаются на экран телефона.
Версия ассетов поднята до `2026-09-14.7`.

## Следующая стадия не начата

- [ ] Выбор набора отпечатков (`convolutionsId`)
- [ ] Валидация пользовательского ввода на шагах 3 и 5
