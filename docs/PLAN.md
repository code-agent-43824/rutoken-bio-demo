# PLAN — текущий этап

**Этап: перевод репозитория на канонический свод правил.**

Источник свода — `code-agent-43824/coding-rules`, файл `AGENTS.md`. Он копируется
в проект как есть и подключается импортом из `CLAUDE.md`; проектное содержимое
живёт отдельно (§0, §2 свода).

- [ ] Положить свод в `AGENTS.md`, подключить строкой `@AGENTS.md` в `CLAUDE.md`
- [ ] Перенести проектное содержимое прежнего `AGENTS.md` в документы по §2:
      требования → `docs/ROADMAP.md`, история этапов → `docs/WORKLOG.md`,
      текущее состояние → `docs/STATUS.md`, деплой и карта кода → `CLAUDE.md`
- [ ] Переписать `CLAUDE.md` на английском — §10 требует этого от файлов-инструкций
- [ ] Обновить `README.md`: ссылка на `AGENTS.md` как на «обязательное чтение»
      больше не верна, история переехала в `docs/`
- [ ] Согласовать с владельцем расхождение по §1: сессия обязана коммитить
      в ветку `claude/zealous-clarke-d5paci`, свод разрешает только транк

Проверка на каждом шаге: `node test.js` — транк должен оставаться зелёным (§1).
