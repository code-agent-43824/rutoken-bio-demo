# PLAN — текущий этап

**Стадия «Правила и документы» закрыта** (`docs/ROADMAP.md`, стадия 5).

- [x] Положить свод в `AGENTS.md`, подключить строкой `@AGENTS.md` в `CLAUDE.md`
- [x] Перенести проектное содержимое прежнего `AGENTS.md` в документы по §2
- [x] Переписать `CLAUDE.md` на английском (§10)
- [x] Обновить `README.md` под новую раскладку документов
- [x] Расхождение по §1 снято: владелец разрешил пушить в транк, ветка влита в `main`

## Следующая стадия не начата

Открытые пункты из `docs/ROADMAP.md` — берутся по указанию владельца, ни один не начат:

- [ ] Проверить биометрические ветки на реальном токене (§13 — проверяет владелец)
- [ ] Выбор набора отпечатков (`convolutionsId`)
- [ ] Валидация пользовательского ввода на шагах 3 и 5
- [ ] **Решение владельца:** загрузчик плагина подключается тегом `<script>` с
      демо-портала Актив без Subresource Integrity. §7 требует пинить внешнее по
      SHA-256, но SRI сломает страницу, как только Актив обновит файл у себя.
      Варианты: оставить как есть и записать решением, добавить SRI и обновлять
      хеш вручную, положить копию загрузчика в репозиторий.
- [!] **За владельцем:** удалить ветку `claude/zealous-clarke-d5paci` на GitHub —
      из сессии не получается (§12, подробности в `docs/JOURNAL.md`)

Проверка на каждом шаге: `node test.js` — транк должен оставаться зелёным (§1).
