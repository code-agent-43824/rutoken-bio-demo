# PLAN — текущий этап

**Этап: переосмысление дизайна и правки по замечаниям владельца.**

Функциональность владельца устраивает; меняются подача, тексты и поведение списка ключей.

## Контент и тексты

- [ ] «Рутокен» везде по-русски — в логотипе шапки латиница
- [ ] Убрать заголовок «Как пользоваться» и подпись под ним — очевидны без слов
- [ ] Убрать из hero обе ссылки-кнопки (на шаги и на журнал)
- [ ] Добавить в hero: первый Рутокен со встроенным отпечатком пальца, запоминает до 3 пальцев
- [ ] Убрать строку про криптопроцессор из карточки устройства
- [ ] Шаг 3: «С использованием биометрии или без использования биометрии»
- [ ] Шаг 4: убрать состояние «не определено» — таких ключей не бывает
- [ ] Шаг 5: «При подписи на ключе с использованием биометрии появится попап»

## Поведение

- [ ] Шаг 2: PIN показывать открытым текстом. **Решение владельца**, не «исправлять»
      обратно на `type="password"` — записать в «Settled decisions»
- [ ] Список ключей обновляется принудительно после создания и после удаления ключа.
      Отменяет прежнее решение «только по кнопке»: его отменил владелец. `test.js`
      проверяет обратное — тест и код правятся одним коммитом (§10)

## Дизайн

- [ ] Пять карточек в две строки (3 + 2) — сейчас пять в ряд, и они слишком узкие
- [ ] Попап переделать: не пытаться рисовать форму токена, а показать отпечаток пальца,
      который сканируется и при успехе становится зелёным
- [ ] Общая подача: типографика, акценты, карточки — менее шаблонно

## Проверка

`node test.js` плюс рендер в Chromium на шести ширинах (`CLAUDE.md`), как в прошлый раз.
Версия ассетов поднимается вместе с правками.

## Следующая стадия не начата

- [ ] Выбор набора отпечатков (`convolutionsId`)
- [ ] Валидация пользовательского ввода на шагах 3 и 5
