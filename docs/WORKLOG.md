# WORKLOG

Новые записи сверху. Одна запись на завершённый кусок работы: план → сделано → дальше.

## 2026-09-14 — чистка фронтенда

**План.** Владелец: «корявенький фронтенд» — проверить, разобраться в причинах,
показать план, починить. Страница отрендерена в Chromium на шести ширинах, замерена
геометрия, найдено восемь дефектов с причинами (`docs/PLAN.md`).

**Сделано.** Все восемь закрыты. Главные: текст в карточке токена наезжал сам на себя;
кнопки биометрии висели поверх hero на средних ширинах и уезжали в подвал на телефоне;
шаги скроллились вбок с обрезанной карточкой; `overflow-x` резал тени карточек.
Кнопки биометрии переехали в шапку — «правый верхний угол» теперь работает на всех
ширинах. `style.css` получил `?v=`, иначе правки дизайна не дошли бы до посетителей.

**Дальше.** Владелец смотрит страницу сам (§13). Открытые пункты — в `docs/PLAN.md`.

## 2026-09-14 — сверка перенесённых фактов

**План.** Владелец указал на ошибки, допущенные при переносе документации. Проверить
всё, что было перенесено из прежнего `AGENTS.md` не глядя в код и историю.

**Сделано.** Нашлось и исправлено:

- в таблице истории стоял коммит `c8a9d2f`, которого нет в репозитории (перенесён
  из прежнего `AGENTS.md`); редизайн — это `3b44192`. Пропущен был `7499819`,
  два коммита стояли не в том порядке. Таблица сверена с `git log`, добавлены даты;
- `STATUS.md` и `CLAUDE.md` копировали значения из кода (версия сборки, PIN по
  умолчанию, число шагов, тексты сообщений) — §2 требует ссылаться по имени;
- `CLAUDE.md` называл PIN по умолчанию «заводским PIN демо-токена» — этого нет
  ни в коде, ни в документах репозитория (§4);
- записи в `JOURNAL.md` подавались как результат наблюдения, хотя восстановлены
  по коду и сообщениям коммитов; источник теперь назван.

**Дальше.** Владелец закрыл два вопроса, оба записаны в «Settled decisions»
в `CLAUDE.md`: PIN по умолчанию оставлен намеренно и не трогается; загрузчик плагина
не пиннится, потому что кастомные плагин и расширение могут меняться в любой день,
а страница должна работать с текущей версией. Он же опроверг причину, которую я
приписал ручному обновлению списка ключей: дело не в скорости, ключи читаются
нормально. Ветку `claude/zealous-clarke-d5paci` из сессии удалить не удалось (§12),
владелец удалил её сам.

## 2026-09-14 — подключение канонического свода правил

**План.** Владелец указал использовать `AGENTS.md` из `code-agent-43824/coding-rules`
как правила кодинга для этого репозитория. Свод предписывает свою раскладку файлов:
`AGENTS.md` — копия свода без правок, `CLAUDE.md` — импорт плюс карта кода,
проектные факты — по шести документам §2. В репозитории на момент начала работы
`AGENTS.md` занят проектной документацией (контекст задачи, история этапов,
инструкция по деплою), а `CLAUDE.md` написан по-русски вопреки §10.

**Сделано.** `AGENTS.md` заменён копией свода. Прежнее содержимое разнесено
по §2: требования и стадии → `ROADMAP.md`, история коммитов → эта запись ниже,
выводы о плагине → `JOURNAL.md`, состояние → `STATUS.md`. `CLAUDE.md` переписан
на английском (§10): импорт `@AGENTS.md`, команды, карта кода, ограничения
тестового стенда, принятые решения с причинами, дисциплина версии и деплой.
`README.md` получил таблицу документов вместо ссылки на прежний `AGENTS.md`.
После каждого шага `node test.js` — зелёный.

**Дальше.** Владелец разрешил пушить в транк, ветка `claude/zealous-clarke-d5paci`
влита в `main` fast-forward. Расхождение по §1 снято и записано в «Settled decisions»
в `CLAUDE.md`. В работе ничего не осталось — следующий шаг за владельцем
(проверка биометрии на реальном токене, `docs/STATUS.md`).

## До 2026-09-14 — создание демо (сводная запись)

Перенесено из прежнего `AGENTS.md` при переходе на канонический свод правил.

**План.** Сделать демо-страницу Рутокен Био по требованиям из
[`ROADMAP.md`](ROADMAP.md): вход по PIN, создание ключей с биометрией и без,
подпись, светлый дизайн rutoken.ru.

**Сделано.**

Сверено с `git log`; в прежнем `AGENTS.md` таблица расходилась с историей.

| Коммит | Дата | Описание |
| --- | --- | --- |
| `cac5897` | 2026-09-10 | Лендинг демо Рутокен Био |
| `319cbfc` | 2026-09-10 | Интеграция с плагином: PIN, создание ключей, подпись, биометрия |
| `b671bfd` | 2026-09-10 | Подробный `AGENTS.md` для преемников и обновлённый `README.md` |
| `3b44192` | 2026-09-10 | Редизайн: светлая тема rutoken.ru, компоновка клиентского пути |
| `bef7c03` | 2026-09-10 | Секция консоли в HTML, привязка `btnClearConsole` |
| `7499819` | 2026-09-10 | Описание секции консоли в `AGENTS.md` |
| `573f2b6` | 2026-09-11 | Исправления интеграции с плагином |
| `cb36397` | 2026-09-11 | Исправление наложений в вёрстке |
| `712c8e5` | 2026-09-14 | Диагностика и поток работы с ключами |
| `26cbe95` | 2026-09-14 | Обработка недоступных счётчиков био-попыток |
| `9130183` | 2026-09-14 | Сброс кэша после правки предварительной проверки биометрии |
| `7dad5cf` | 2026-09-14 | Аутентификация операций управления био-ключами |

Ключевые выводы этапа интеграции — в [`JOURNAL.md`](JOURNAL.md).

**Дальше.** Проверка биометрических веток на реальном токене; см. [`STATUS.md`](STATUS.md).
